# Kismet site override configuration for CyberShield
# This file overrides default Kismet settings

# HTTP server settings
httpd_username=kali
httpd_password=kali
httpd_port=2501

# Disable remote capture to avoid port conflicts
remote_capture_listen=0.0.0.0
remote_capture_port=0

# GPS settings (disable if not needed)
gps=false
